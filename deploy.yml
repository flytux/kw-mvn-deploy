---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kw-mvn
spec:
  selector:
    matchLabels:
      app: dev
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: dev
    spec:
      containers:
        - name: kb-mvn
          image: docker.vm01/kw-mvn:dev-211105-145153-f7755115
          imagePullPolicy: IfNotPresent
          env:
            - name: TZ
              value: Asia/Seoul
