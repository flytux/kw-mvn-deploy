---
apiVersion: v1
kind: Service
metadata:
  name: kw-mvn
  labels:
    app: dev
spec:
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30008
      name: http-port
  type: NodePort
  selector:
    app: dev
