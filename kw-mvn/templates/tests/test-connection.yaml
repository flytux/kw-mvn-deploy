apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kw-mvn.fullname" . }}-test-connection"
  labels:
    {{- include "kw-mvn.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kw-mvn.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
